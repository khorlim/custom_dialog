part of 'custom_dialog.dart';

enum AlignTargetWidget {
  right,
  bottomCenter,
  bottomLeft,
  left,
  topCenter,
  rightCenter,
  leftCenter,
  centerBottomRight,
}

enum ArrowPointing { top, right, left, bottom }
